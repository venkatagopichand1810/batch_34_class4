// Function expression is also used to define the function and assign to a variable

const isEven = function(number) {
    return number % 2 === 0
}

console.log(isEven(10))

// IIFE(immediately invoked function expression)

